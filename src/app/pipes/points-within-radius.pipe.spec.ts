import { PointsWithinRadiusPipe } from './points-within-radius.pipe';

describe('PointsWIthinRadiusPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsWithinRadiusPipe();
    expect(pipe).toBeTruthy();
  });
});
